<h1>Edit Sponsored Post</h1>

 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for Sponsored Posts:</p>
     <ul>
       <li>Third person narrative is required.</li>
       <li>Must meet <b>minimum</b> of seventeen adjectives.</i>.</li>
       <li>Emojis will not be permitted.</li>
     </ul>
   </div>

     <div class="col-md-8">
       <%= form_for [@sponsored_post.topic, @sponsored_post] do |f| %>
         <div class="form-group">
           <%= f.label :title %>
           <%= f.text_field :title, class: 'form-control', placeholder: "Enter title of sponsored post" %>
         </div>
         <div class="form-group">
           <%= f.label :body %>
           <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter post content" %>
         </div>
         <div class="form-group">
           <%= f.label :price %>
           <%= f.number_field :price, class: 'form-control', placeholder: "Price tag" %>
         </div>
         <div class="form-group">
           <%= f.submit "Save", class: 'btn btn-success' %>
         </div>
       <% end %>
     </div>
   </div>
